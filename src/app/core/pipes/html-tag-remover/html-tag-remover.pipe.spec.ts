import { HtmlTagRemoverPipe } from './html-tag-remover.pipe';

describe('HtmlTagRemoverPipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlTagRemoverPipe();
    expect(pipe).toBeTruthy();
  });
});
