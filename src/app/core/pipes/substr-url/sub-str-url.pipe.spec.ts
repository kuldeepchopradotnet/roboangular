import { SubStrUrlPipe } from './sub-str-url.pipe';

describe('SubStrUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new SubStrUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
